# Лабораторная работа №4: Разработка плагина для WordPress

**Студент:** Виктор Купчишин

**Группа:** IA2303

## Цель работы
Освоить расширяемую модель данных WordPress: создать Custom Post Type (CPT), пользовательскую таксономию, метаданные с метабоксом в админ-панели, а также реализовать вывод данных на сайте с помощью шорткода.

---

## Описание лабораторной работы
В рамках работы был разработан учебный плагин **USM Notes**, который добавляет на сайт раздел «Заметки» с возможностью присваивать каждой заметке **приоритет** и **дату напоминания**. Основные элементы плагина:

- **Custom Post Type (CPT)** – `usm_note` для заметок.
- **Пользовательская таксономия** – `priority` для приоритета (High, Medium, Low).
- **Метабокс** – поле выбора даты напоминания (`Reminder Date`) с валидацией.
- **Шорткод** – `[usm_notes]` для вывода заметок на страницах сайта с фильтрацией по приоритету и дате.

---

## Инструкции по запуску проекта

1. Скопировать папку плагина `usm-notes` в директорию `wp-content/plugins` локальной установки WordPress.
2. В файле `wp-config.php` включить отладку:
   ```php
   define('WP_DEBUG', true);

3. Зайти в **Админка → Плагины**, найти **USM Notes** и активировать.
4. Создать несколько заметок через **Notes → Add New**, заполняя:

   * Заголовок и текст
   * Приоритет (High / Medium / Low)
   * Дату напоминания (`Reminder Date`)
5. Создать страницу сайта (например, «All Notes») и вставить шорткод для отображения заметок.

---

## Краткая документация к плагину

### Custom Post Type

* **Название:** Notes
* **Поддержка:** title, editor, author, thumbnail
* **Архив:** есть
* **Иконка в админке:** dashicons-edit

### Таксономия

* **Название:** Priorities
* **Иерархическая:** Да
* **Слова для выбора:** High, Medium, Low

### Метабокс

* **Название поля:** Reminder Date
* **Тип:** HTML5 input type="date"
* **Валидация:** обязательное поле, дата не может быть в прошлом

### Шорткод

* `[usm_notes]` – вывод всех заметок
* `[usm_notes priority="high"]` – заметки с приоритетом High
* `[usm_notes before_date="2025-04-30"]` – заметки с датой напоминания до 30 апреля 2025
* `[usm_notes priority="high" before_date="2025-04-30"]` – комбинация фильтров

---

## Примеры использования плагина

1. **Вывод всех заметок на странице:**

   ```text
   [usm_notes]
   ```

2. **Вывод заметок с высоким приоритетом:**

   ```text
   [usm_notes priority="high"]
   ```

3. **Вывод заметок с датой напоминания до 30 апреля 2025:**

   ```text
   [usm_notes before_date="2025-04-30"]
   ```

4. **Комбинированный фильтр (High и до 30 апреля 2025):**

   ```text
   [usm_notes priority="high" before_date="2025-04-30"]
   ```

Если фильтры не соответствуют ни одной заметке, выводится сообщение:
**"Нет заметок с заданными параметрами"**

---

## Контрольные вопросы

1. **Что такое CPT и зачем он нужен?**
   CPT (Custom Post Type) — это пользовательский тип записи в WordPress, который позволяет создавать собственные разделы на сайте, отличные от стандартных Постов и Страниц.

2. **Как создать пользовательскую таксономию?**
   Через функцию `register_taxonomy()`, указав объект (CPT), метки и параметры публичности и иерархичности.

3. **Что такое метабокс и как он используется?**
   Метабокс — это блок на странице редактирования записи, который позволяет добавлять и сохранять дополнительные данные (метаданные). Для этого используют функции `add_meta_box()` и `save_post`.

4. **Как работает шорткод?**
   Шорткод — это специальный тег `[usm_notes]`, который WordPress заменяет на динамический контент (список заметок с фильтрацией).

5. **Как реализована валидация даты напоминания?**
   Дата проверяется при сохранении записи: поле обязательно и дата не может быть меньше текущей. Если проверка не пройдена, данные не сохраняются.

---

## Список использованных источников

1. [WordPress Codex – register_post_type](https://developer.wordpress.org/reference/functions/register_post_type/)
2. [WordPress Codex – register_taxonomy](https://developer.wordpress.org/reference/functions/register_taxonomy/)
3. [WordPress Codex – add_meta_box](https://developer.wordpress.org/reference/functions/add_meta_box/)
4. [WordPress Codex – add_shortcode](https://developer.wordpress.org/reference/functions/add_shortcode/)
5. Официальная документация WordPress по Custom Post Types, Taxonomies и Meta Boxes

---

## Дополнительные важные аспекты

* Все функции плагина выполняются через хуки (`init`, `add_meta_boxes`, `save_post`, `wp_head`) для корректного подключения и безопасности.
* Используется проверка **nonce** при сохранении метаданных для защиты от CSRF.
* CSS встроен через `wp_head` для упрощения отображения списка заметок.



